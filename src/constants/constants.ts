export enum StatusCode {
    'OK' = 200,
    'Create' = 201,
    'NotFound' = 404,
    'BadRequest' = 400
}

export enum StatusMessage {
    'OK' = 200,
    'Create' = 201,
    'NotFound' = 404,
    'BadRequest' = 400
}
